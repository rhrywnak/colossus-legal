import React from "react";

const Decisions: React.FC = () => (
  <div>
    <h2>Decisions</h2>
    <p>This is the placeholder page for Decisions. CRUD table will be added in a later phase.</p>
  </div>
);

export default Decisions;
