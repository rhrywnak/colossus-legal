import React from "react";

const Claims: React.FC = () => (
  <div>
    <h2>Claims</h2>
    <p>This is the placeholder page for Claims. CRUD table will be added in a later phase.</p>
  </div>
);

export default Claims;
