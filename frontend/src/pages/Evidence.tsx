import React from "react";

const Evidence: React.FC = () => (
  <div>
    <h2>Evidence</h2>
    <p>This is the placeholder page for Evidence. CRUD table will be added in a later phase.</p>
  </div>
);

export default Evidence;
