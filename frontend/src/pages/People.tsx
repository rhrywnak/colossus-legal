import React from "react";

const People: React.FC = () => (
  <div>
    <h2>People</h2>
    <p>This is the placeholder page for People. CRUD table will be added in a later phase.</p>
  </div>
);

export default People;
