# Colossus-Legal frontend .env.example
# Copy this file to .env and adjust values as needed.

# Backend base URL for API calls
VITE_API_URL=http://localhost:3000

# Timeout (ms) for long-running pipeline operations (extract, verify, ingest, etc.)
VITE_PIPELINE_LONG_TIMEOUT_MS=600000
