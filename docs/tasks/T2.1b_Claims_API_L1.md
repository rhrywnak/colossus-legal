# Task: T2.1b – Claims API L1 (Real Neo4j List, Happy Path)

title: "T2.1b – Claims API L1 (Real Neo4j List, Happy Path)"
instructions:
  This task upgrades the Claims API from L0 (stubs) to L1 (real Neo4j-backed data).

  The goal is:
    - `GET /claims` returns real Claim data from Neo4j.
    - Happy path works end-to-end.

  Validation and detailed error handling will be handled in L2 (T2.1c).

context:
  repo: colossus-legal
  persona: BackendAgent
  backend-root: ./backend
  branch: feature/T2.1a-claims-api-l0
  docs:
    - AGENTS.md
    - docs/WORKFLOW.md
    - docs/DEV_ONBOARDING.md
    - docs/TASK_TRACKER_V2.md
    - docs/ARCHITECTURE.md
    - docs/API_DESIGN.md
    - docs/DATA_MODEL.md

requirements:
  - Implement a `Claim` domain model in `backend/src/models/claim.rs` (if not already present).
  - Implement `ClaimRepository::list_claims()` in `backend/src/repositories/claim_repository.rs`:
      - Use a Cypher query such as `MATCH (c:Claim) RETURN c`.
      - Map the returned Node to the `Claim` model.
  - Update `list_claims` handler in `backend/src/api/claims.rs` to:
      - Use `ClaimRepository` with `AppState` / Neo4j `Graph`.
      - Map `Vec<Claim>` into `Vec<ClaimDto>`.
  - Keep error handling simple for now:
      - Repository errors can be mapped to `StatusCode::INTERNAL_SERVER_ERROR`.
  - Do not add advanced validation or relationship logic; that is L2+.
  - Run:
      - `cargo check --manifest-path backend/Cargo.toml`
      - `cargo run --manifest-path backend/Cargo.toml`
      - `curl http://localhost:3403/claims` to verify non-empty and empty graph behavior, as appropriate.

tests:
  - Required (Integration-first).
  - Add an integration test file, e.g., `backend/tests/claims_list.rs`:
      - Test 1: With one or more Claim nodes present in Neo4j, `GET /claims` returns 200 and a non-empty JSON array.
      - Test 2: With no Claim nodes, `GET /claims` returns 200 and an empty array.
      - Tests should insert/delete test data via Cypher in a setup/teardown section.
  - Ensure tests run via `cargo test --tests --manifest-path backend/Cargo.toml` (or similar command).

acceptance_criteria:
  - `GET /claims` uses Neo4j to return Claim nodes (no stubs).
  - Happy path works end-to-end in a dev environment.
  - At least one backend integration test exists and passes.
  - No changes to `/health` or `/api/status` semantics.
  - `docs/TASK_TRACKER.md` updated to reflect T2.1b status.

files:
  - backend/src/models/claim.rs
  - backend/src/repositories/claim_repository.rs
  - backend/src/api/claims.rs
  - backend/src/api/mod.rs
  - backend/src/state.rs
  - backend/tests/claims_list.rs
  - docs/TASK_TRACKER_V2.md
