# Task: T2.2a – Claims UI L0 (Skeleton Page + Stub Service)

title: "T2.2a – Claims UI L0 (Skeleton Page + Stub Service)"
instructions:
  This task implements **Layer 0** for the Claims UI.

  The goal is:
    - A `/claims` route exists in the frontend.
    - A `ClaimsPage` renders with stubbed data using a local mock service.
  Do NOT call the real backend yet; that will be handled in T2.2b.

context:
  repo: colossus-legal
  persona: FrontendAgent
  frontend-root: ./frontend
  branch: feature/frontend-claims-ui
  docs:
    - AGENTS.md
    - docs/TASK_TRACKER.md
    - docs/ARCHITECTURE.md
    - docs/API_DESIGN.md

requirements:
  - Create `frontend/src/services/claims.ts`:
      - Export a `getClaimsStub()` function that returns a Promise of a hard-coded array.
  - Create `frontend/src/pages/ClaimsPage.tsx`:
      - Uses `useEffect` + `useState` with `getClaimsStub()`.
      - Shows loading, error, and list states.
  - Add route `/claims` in `App.tsx` (or router):
      - Route renders `ClaimsPage`.
  - Run:
      - `npm install` (if not already done)
      - `npm run build`
      - `npm run dev` and manually verify `/claims`.
  - Update `docs/TASK_TRACKER.md`:
      - Mark T2.2a as `IN_PROGRESS` or `DONE`.

acceptance_criteria:
  - Frontend builds successfully.
  - Visiting `/claims` shows a list of stubbed claims.
  - No network calls to the real backend yet.
  - No changes to other pages beyond basic navigation.

files:
  - frontend/src/services/claims.ts
  - frontend/src/pages/ClaimsPage.tsx
  - frontend/src/App.tsx
  - docs/TASK_TRACKER.md
