# Task: T2.2b – Claims UI L1 (Real API Integration + Basic Tests)

title: "T2.2b – Claims UI L1 (Real API Integration + Basic Tests)"
instructions:
  This task upgrades the Claims UI from L0 (stub service) to L1 (real API integration).

  The goal is:
    - ClaimsPage calls the real `GET /claims` backend endpoint.
    - Basic tests exist covering loading, success, and error states.

context:
  repo: colossus-legal
  persona: FrontendAgent
  frontend-root: ./frontend
  docs:
    - AGENTS.md
    - docs/WORKFLOW.md
    - docs/DEV_ONBOARDING.md
    - docs/TASK_TRACKER_V2.md
    - docs/API_DESIGN.md

requirements:
  - Update `frontend/src/services/claims.ts`:
      - Switch from stubbed data to `fetch` calls to `${VITE_API_URL}/claims`.
  - Update `frontend/src/pages/ClaimsPage.tsx`:
      - Use the real `getClaims` function.
      - Display:
          - Loading state while fetching.
          - Empty state when no claims are returned.
          - Error message when the fetch fails.
  - Run:
      - `npm run build` (ensure no type or build errors).
      - `npm run dev` for manual verification.

tests:
  - Required for L1.
  - Add a Vitest test file, e.g., `frontend/src/services/__tests__/claims.test.ts`:
      - Mock `fetch` to return:
          - Non-empty claims array.
          - Empty array.
          - Error response.
      - Assert that `getClaims()` handles these scenarios correctly.
  - Optionally, add a React Testing Library test for ClaimsPage verifying:
      - Loading state.
      - Empty state.
      - Successful render with mocked data.

acceptance_criteria:
  - ClaimsPage uses the real backend API rather than stubbed services.
  - Basic service-level tests exist and pass.
  - Frontend build is successful.
  - TASK_TRACKER_V2 updated for T2.2b status.

files:
  - frontend/src/services/claims.ts
  - frontend/src/pages/ClaimsPage.tsx
  - frontend/src/services/__tests__/claims.test.ts
  - docs/TASK_TRACKER.md
